<?xml version="1.0" encoding="ISO-8859-1" ?>
<configuration>
  <header>
    <!-- Meta data about the resulting combined configuration -->
  </header>
  <override>
    <!-- Configuration declarations with override semantics -->
    <xml fileName="theme.xml" delimiterParsingDisabled="true" config-name="theme" config-optional="false"/>    
    <system />
  </override>
  <additional>
    <!-- Configuration declarations that form a union configuration -->
  </additional>
</configuration>